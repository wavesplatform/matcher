# Base configuration for waves nodes for dex-it tests. It is assumed that this configuration does not change
include "waves-base.conf"

# Node name to send during handshake
waves.network.node-name = "node01"

# Configuration of the waves.blockchain.custom.genesis/address-scheme-character sections
# generated by GenesisConfigGenerator from dex-it/src/test/resources/genesis.conf
include "run.conf"

# Highest priority configurations specified in tests (suiteInitialWavesNodeConfig)
include "suite.conf"